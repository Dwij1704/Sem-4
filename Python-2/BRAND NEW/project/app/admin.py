from django.contrib import admin
from .models import *
# Register your models here.
admin.site.register(user)   # Registering the model user in admin site
admin.site.register(article)   # Registering the model user in admin site